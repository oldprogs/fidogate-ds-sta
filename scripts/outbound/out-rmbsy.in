#!<SHELL>
#
# $Id: out-rmbsy.in,v 1.1 2003/11/05 00:47:51 rusfidogate Exp $
#
# Remove BSY files in outbound, older than 4 hours
#
# Needs GNU find!
#

OUT="<BTBASEDIR>/out"

if [ $# == 0 ]; then
    find $OUT -follow -name '*.bsy' -mmin +240 -print -exec rm -f {} \; 2>/dev/null
elif [ $1 == "now" ]; then
    rm -f `find $OUT -follow -name *.bsy`
fi
