#!<SHELL>
#
# $Id: rc.fidogate.in,v 5.1 2004/12/10 19:09:55 anray Exp $
#
# FIDOGATE boot cleanup
#

# Remove lock files
rm -f <LOCKDIR>/*

# Remove .tmp files
rm -f <SPOOLDIR>/outpkt/*.tmp <SPOOLDIR>/outpkt/*/*.tmp
rm -f <SPOOLDIR>/toss/*/*.tmp
